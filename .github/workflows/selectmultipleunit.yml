name: selectmultipleunit

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        os: [win2019, win2022]
        selected: [true, false]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build
        run: |
          if ( "${{ matrix.selected }}" == "true" ); then
            echo "running job1"
          else
             echo "running job2"
          fi

  
