name: selectmultipleunit

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        os: [win2019, win2022]
        selected: [true, false]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build
        run: |
          if ( "${{ matrix.selected }}" == "true" ); then
            # Build commands for the selected matrix
          else
            # Build commands for the non-selected matrix
          fi

  
