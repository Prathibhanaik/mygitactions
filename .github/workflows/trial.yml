name: trial

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: if: github.event.inputs.deploy == 'true'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to environment
        run: |
          echo "Deploying to ${{ github.event.inputs.environment }}"
