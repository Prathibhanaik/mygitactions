name: trial

on:
  workflow_dispatch:
    inputs:
      enable-checkbox:
        description: 'Enable checkbox'
        default: ${{ github.event.inputs.enable-condition == 'true' }}
        required: false

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Do something
        run: echo 'Hello world!'

